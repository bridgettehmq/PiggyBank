{{#if isLoggedIn}}

          <div class="card-app">
        <div class="card mx-auto text-center">
          <div class="card-body">
            <h2>Welcome username</h2> {{user.username}}
                      <p>you have $ {{user.balance}}</p>
          </div>
        </div>
        <div class="form-group form-check">
                               
        </div>
        <div class="d-grid gap-2 col-6 mx-auto">
          <div class="card-body text-center">
      <form class="mb-2" id="money">
             <div class="form-group">
              <label> Deposit </label>
              <input type="text" class="form-control" id="userDeposit"  placeholder="$ 00.00">
             </div>
                   </form>
           </div>
           <button type="submit" class="btn btn-primary">click here to add more</button>
           <div class="card-body text-center">
           
      <form class="mb-2" id="money">
                    <div class="form-group">
                        <label> Withdrawl </label>
                        <input type="text" class="form-control" id="userWithdrawl"  placeholder="$ 00.00">
                    </div>

                </form>
            </div>
          <button type="submit" class="btn btn-primary">click here to substract</button>
          
               </div>
      </div>
      {{/if}}

